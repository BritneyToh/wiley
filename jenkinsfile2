pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh 'Pythontestfile'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh 'Pythontestfile'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                sh 'Pythontestfile'
            }
        }
    }
}
